# Community Issue Reporter Backend

FastAPI backend for chatbot logic, Supabase, and Relay integration.

## Usage

- Install dependencies: `pip install fastapi uvicorn supabase`
- Run server: `uvicorn main:app --reload`
